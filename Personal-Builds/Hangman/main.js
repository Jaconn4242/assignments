const alphabet = document.getElementsByClassName("alphabet")
console.log(alphabet)

for(i=0; i < alphabet.length; i++){
    let results = new Array()
    results.push(alphabet[i])
}
console.logo(results)